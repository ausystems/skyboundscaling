# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor / tooling
.vscode/
.idea/
*.swp

# Local agent + preview config (not part of the site)
.claude/

# Vercel local state
.vercel

# Logs
*.log
npm-debug.log*

# NOTE: nothing under assets/ is ignored. The site is plain static HTML and
# every file in assets/ (css, js, fonts, images, brand, icons) must ship, or
# the deployed pages render unstyled. Do not add rules like *.min.js,
# dist/, build/, or assets/ here.
